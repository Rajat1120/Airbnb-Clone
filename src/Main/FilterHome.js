import React from "react";

const FilterHome = () => {
  return (
    <div className={"w-[30%]    border-blur    rounded-lg overflow-hidden"}>
      <button>Filter</button>
      <button>Display total before taxes</button>
    </div>
  );
};

export default FilterHome;
